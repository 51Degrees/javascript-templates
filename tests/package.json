{
  "name": "javascript-templates-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Checks that render JavaScriptResource.mustache and drive the rendered script.",
  "scripts": {
    "test": "node template-tests.js"
  },
  "devDependencies": {
    "mustache": "4.2.0"
  }
}
